origin https://tum-ustp@github.com/tum-ustp/InclusiveSpacesCAT.git
user.name tum-ustp
user.email inclusivespaces.svp@ed.tum.de

git config user.name "ritaMZ"
git config user.email "margarita.zykova@tum.de"

git config --local user.name
git config --local user.email

git config --unset user.name
git config --unset user.email

git remote set-url origin https://github.com/tum-ustp/InclusiveSpacesCAT.git
git remote -v
